# Cadence Modus
#   Cadence(R) Modus(TM) DFT Software Solution, Version 20.12-s002_1, built Feb 26 2021 (linux26_64)
#   Date: Monday, March 16, 2026
set_db workdir /projects/2025H1230098P/cadence/FIFO/test_scripts
# Write all faults to a text file
report_faults -all -output reports/full_fault_report.txt
# Or check only the "bad" ones (Untestable or Blocked)
report_faults -all -output reports/full_fault_report.txt
report_faults -type all -file reports/full_fault_report.txt
report_statistics
report_faults -type all > reports/full_fault_report.txt
report_faults -type all > reports/full_fault_report.txt
report_statistics
build_fault_statistics
write_statistics -file reports/atpg_stats.txt
report_summary > reports/atpg_summary.txt
